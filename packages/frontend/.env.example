# ===========================================
# Frontend - Storacha Integration (Issue #3)
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit .env with real credentials

# Storacha account email (required for PoC testing)
# You will receive a verification email when running the integration script
STORACHA_EMAIL=your-email@example.com

# Storacha Space DID (optional - reuse existing space to avoid quota issues)
# After first run, copy the Space DID from output and set it here
# Format: did:key:z6Mk... (long string)
# Leave empty to create new space on each run
STORACHA_SPACE_DID=
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=